# [#08] 薙刀式.txt 直読インポータ（上流追従）

- **優先度:** P3
- **状態:** 🗄 Backlog
- **依存:** #02
- **関連設計:** 詳細設計書 B.3 / 要件 IR-3

## 目的
薙刀式 `.txt`（やまぶき形式）を build.rs が**直接読んで**レイヤテーブルを生成できるようにし、
v15以降の上流更新に追従しやすくする（手動YAML転記#04を自動化）。

## スコープ（やること）
- [ ] `.txt`（Shift-JIS）パーサ：`-option-input` のキー→sc 定義
- [ ] `[ … ]` 位置テーブル / `({トリガ}[ … ]` レイヤブロックの解釈
- [ ] `{S}` センターシフト・後置/二重シフトの表現を内部レイヤモデルへマップ
- [ ] `{vkF2}`/`{Enter}`/`^c` 等の制御を Action::Keys へ
- [ ] build.rs から YAML 経路と切替可能に（どちらをソースにするか選択）

## やらないこと
- 配列仕様そのものの変更（#02のモデルを使う）

## 受入条件（Done の定義）
- [ ] `.txt` から生成した LAYERS が、手動YAML(#04)と機能的に一致するサンプル検証
- [ ] Shift-JIS 文字の取り扱いが正しい

## 実装メモ / 参照
- 出典: `Documents/NaginataShiki/Nagitanashiki_v15fix_version*.txt`
- やまぶき形式の文法（位置=QWERTY順、`|`区切り、`+sc`、`{ }`エスケープ）を要把握
- 完成すれば #04 の手作業を大幅削減。ただし文法網羅コストとのトレードオフ
